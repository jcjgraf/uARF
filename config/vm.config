HOSTNAME := $(shell hostname)

ifeq ($(HOSTNAME), jcarch)
    KDIR=/home/jeanclaude/Documents/Programming/LinuxKernelTorvalds
else ifeq ($(HOSTNAME), ee-tik-cn128)
    KDIR=/local/home/jegraf/LinuxKernelTorvalds
else
    $(warning "Unrecognised system")
endif
