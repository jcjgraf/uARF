SRC="$HOME/CurrentCourse/Code/scanner/" # Trailing slash matters!
DST="cn128:~/scanner"

EXCLUDE_BASE=".git .cache .venv"
EXCLUDE_EXTENDED=""

WATCH_CMD=(
    find "$SRC" \
    -type f \
    -name "\*.py" \
)

WATCH=$(eval ${WATCH_CMD[@]})
