SRC="$HOME/CurrentCourse/Code/g2h/" # Trailing slash matters!
DST="cn128:~/g2h"

EXCLUDE_BASE=".git .cache"
EXCLUDE_EXTENDED="\
    *.o\
    *.d\
    *.so\
"

WATCH_CMD=(
    find "$SRC" \
    -type f \
    -name "\*.c" \
    -or -name "\*.h" \
    -or -name "\*.S" \
    -or -name "Makefile" \
    # -or -name "\*.sh" \
)

WATCH=$(eval ${WATCH_CMD[@]})
